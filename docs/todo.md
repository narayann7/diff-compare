need to add other options : toggle options 
currently 
- ignore whitescpace
- minmap view

new (lets add)
- Ignore case
- Ignore empty lines
- Ignore line endings (LF vs CRLF)


lets create a setting icon on side of expand/collape button
clicking on that we can configure all above features 